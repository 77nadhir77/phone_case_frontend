export type Address = {
    id?: number,
    name: string,
    street: string, 
    city?: string, 
    state: string,
    zipcode: string, 
    createdAt?: string,
	updatedAt?: string,
    phone: number, 
    email: string, 
}